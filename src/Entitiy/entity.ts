import { TodoEntity } from './todo.entity';
import { UserEntity } from './user.entity';

const entities = [TodoEntity, UserEntity];

export { TodoEntity, UserEntity };

export default entities;
